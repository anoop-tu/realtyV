// Layout wrapper for all admin pages in RealtyView
// Provides a consistent layout for admin routes
export default function AdminLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
